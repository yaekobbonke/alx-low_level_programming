#include "main.h"
#include <stdio.h>

/**
 * _strlen - check the code for main.h
 * @s: declaration of *s and paramters for the function _strlen
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
return (0);
}
